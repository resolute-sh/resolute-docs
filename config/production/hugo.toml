# Overrides for production environment
# baseurl is set via HUGO_BASEURL env var in deployment platform
# For resolute.sh: https://resolute.sh/docs/
# For Vercel preview: https://resolute-docs.vercel.app/
